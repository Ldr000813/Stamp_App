# デジタルスタンプラリー QAレポート

- **対象アプリ**: 京都まちめぐりスタンプラリー（OPEN CITY LABO / Meetup.kyoto）
- **本番URL**: https://stamp-app-two.vercel.app
- **技術**: Next.js 14 (App Router) / Supabase (Postgres・Auth・Storage・RLS) / Vercel
- **テスト日**: 2026-09-25
- **テスト方法**: 本番環境に対するブラウザ実操作テスト＋API直接検証＋ロジックの自動テスト(Vitest)

---

## 総評

主要フロー・権限分離・データ整合性・多言語まで本番で動作確認済み。実運用に向けた状態は良好。
QA中に発見した課題（重大1件・軽微3件）はすべて修正・再検証まで完了。

---

## 発見した課題と対応

| # | 深刻度 | 内容 | 原因 | 対応 | 状態 |
|---|---|---|---|---|---|
| 1 | 重大 | 追加・取得した内容（新クーポン、スタンプ、特典進捗）がユーザー画面に反映されない | Next.jsのデータキャッシュがSupabaseの読み取りfetchをキャッシュしていた（`dynamic="force-dynamic"`だけでは防げない内部fetchキャッシュ） | `supabaseAdmin`の全読み取りに`cache:"no-store"`を適用（`src/lib/supabaseServer.ts`） | 修正・再検証済 |
| 2 | 軽微 | スタンプ完了画面が「N / 5 個」と廃止済みの既定値を表示 | 旧campaign.required_stampsを参照 | 「集めたスタンプ: N」の累計表示に変更（`src/app/stamp/page.tsx`） | 修正・再検証済 |
| 3 | 軽微 | スポット詳細ページにその店舗のイベント予定が表示されない | 詳細ページがイベントを取得していなかった | `/api/events?spotId=`を追加し、詳細ページに「イベント予定」欄を追加（`src/app/api/events/route.ts`, `src/app/spot/[id]/page.tsx`） | 修正・再検証済 |
| 4 | 軽微(UX) | QR発行が`window.open`のポップアップ方式でブラウザにブロックされ得る | 別ウィンドウ生成に依存 | ページ内モーダルでQR表示（画像保存/閉じる付き）に変更（`src/app/admin/page.tsx`） | 修正・再検証済 |
| 5 | 情報 | 管理画面の一覧が追加直後に一瞬古く見える | クライアント側の再取得タイミング | データは正常。再読込で解消。経過観察 | 対応不要 |

### 課題1の技術メモ（重要・再発防止）
サーバー側でSupabaseに読み取りをかける際、Next.jsが内部の`fetch()`をData Cacheにのせるため、ユーザー向けAPIが古い結果を返していた。
決め手は「まっさらな新規参加者ID（未キャッシュのURL）では毎回正しく動く」一方、既存アカウント/既存URLだけ古い値が返っていたこと。
`createClient(url, key, { global: { fetch: (u,i)=>fetch(u,{...i, cache:"no-store"}) } })` で全読み取りをキャッシュ無効化して解消。ローカル開発や単体テストでは再現しない“本番だけの不具合”。

---

## 正常動作を確認した項目（本番ライブ）

### 基盤・認証
- デプロイ／環境変数／Supabase接続／コンソールエラーなし
- ログイン必須ゲート（未ログインはログイン画面）
- 日本語／英語の切替（全画面）

### 参加者フロー
- スタンプ取得（新規）→ 取得時刻＝現在、🎉表示
- 重複防止：同一スポット2回目は「取得済み」、件数増えず（ユニーク制約有効）
- スナップショット記録：スタンプ行に取得スポット名・画像・日時を保存
- ホーム：特典ごとのスタンプカード（マス目グリッド）を作成日時の時系列順に表示
- 獲得済みマスのタップ → 「いつ・どのスポットで取得したか」の詳細モーダル
- 特典カウント：**その特典の作成時刻より後に押したスタンプのみ**加算（20個特典=2、1個特典=1で解放を実測）
- 特典解放UI（「獲得！」バッジ・受け取り方法表示）

### クーポン
- 一般的なクーポンUIで表示
- スライド使用：ノブを右端までドラッグ → 「使用済み」＋USEDスタンプ＋使用日時
- 使用状態の永続（再読込・別ページ遷移後も維持、再使用不可）

### 閲覧系ページ
- 対象スポット：丸写真グリッド（獲得済みでも✓オーバーレイは付かない＝仕様どおり）
- スポット詳細：写真・名前・説明・イベント予定・道順（Googleマップリンク）
- イベントフィード：日付ごと・当日バッジ・会場・道順
- 地図：Leaflet＋OpenStreetMap（座標なし=リンクのみスポットはピン無し＝仕様どおり）
- 注意事項ページ

### 管理者
- 管理者ログイン（ADMIN_EMAILS、共有セッションで参加者/オーナー/管理者を横断）
- スポット追加（owner_email・map_url・画像の保存）
- 特典の追加・必要スタンプ数の設定
- クーポンの追加
- QR発行（ページ内モーダルでQR画像＋URL＋画像保存）
- 完全削除（GitHub風のランダムコード確認）

### オーナー
- オーナー画面が自分の担当店舗を表示（管理者は全店舗＋「運営」バッジ）
- 各店舗のイベントを追加・編集・削除

### 権限分離・セキュリティ
- 未認証で管理・オーナーAPI（8種）すべて401
- RLSにより`stamps`は匿名で読めない
- SUPABASE_SERVICE_ROLE_KEYはサーバー限定（クライアント露出なし）

### データ整合性（最重要）
- **完全削除してもスタンプは消えない**（本番実地で検証）
  - テストスポット作成 → スタンプ取得 → コード入力で完全削除
  - 結果：スポットは物理削除されても、取得済みスタンプは残存／スナップショット名を保持／`spot_id`はnull／紐づく特典は解放状態を維持
- 特典進捗は「作成時刻より後のスタンプのみ」で正しく算出
- 同一参加者・同一スポットの二重登録は発生しない

---

## 自動テスト（Vitest）

`npm test` で全20項目PASS。純粋ロジックを`src/lib/`に切り出して検証。

- `rewards.ts` … 特典の進捗計算（作成時刻より後のスタンプのみカウント）
- `apiAuth.ts` … 管理者判定・オーナー権限判定（権限分離の中核）
- `datetime.ts` … 日時の変換・表示
- `mapurl.ts` … GoogleマップURLからの座標抽出（実ピン優先）

---

## 未実施・制約事項

- **オーナーの他店舗不可**を別アカウントで実地確認：ブラウザ操作ではパスワード入力ができないため未実施。API 401＋`canManageSpotDecision`の単体テストで論理は担保。
- CSVエクスポートのダウンロード実行、メール確認付き新規登録フロー：未実施。
- スマホ実機でのQR物理読み取り・カメラ挙動：未実施（実機推奨）。

---

## 本番リリース前の推奨アクション

1. テスト用の残置データ（テスト参加者の重複スタンプ等）をクリアしてクリーンな状態にする。
2. `ADMIN_EMAILS`は運営メンバーのみに限定（全店舗の全権を持つため）。
3. Supabaseの無料枠→本番規模での有料枠移行を検討（同時接続・自動停止の観点）。
4. 別アカウントでのオーナー権限の実地確認を一度行う。

---

## 変更ファイル一覧（今回のQAで修正）

- `src/lib/supabaseServer.ts` — 読み取りキャッシュ無効化（課題1）
- `src/app/stamp/page.tsx` — 完了表示を累計に（課題2）
- `src/app/api/events/route.ts` — `?spotId=`対応（課題3）
- `src/app/spot/[id]/page.tsx` — イベント予定欄を追加（課題3）
- `src/app/admin/page.tsx` — QRをページ内モーダル化（課題4）
